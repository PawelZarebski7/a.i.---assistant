export const metadata = {
  title: 'Mobi AI Assistant',
  description: 'Dashcode is a popular dashboard template.',
}

const layout = ({ children }) => {
  return (
    <>
      {children}
    </>
  );
};

export default layout;